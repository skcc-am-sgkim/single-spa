import Theme from "./theme";

export default {
  title: "Components/Theme",
  component: Theme,
  parameters: {
    layout: "centered",
  },
  argTypes: {},
};

export const Default = {
  args: {
    mode: true,
  },
};
